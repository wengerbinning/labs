


.. code-block:: shell
    mkdir $cwd && cd $cwd
    npm init -y


.. code-block:: shell
    npm install vue
    npm install -D vite
    npm install -D @vitejs/plugin-vue











#
npm config ls -l




* 运行时依赖: npm install -S(--save)
* 开发时依赖: npm install -D(--save-dev)
