ifeq ($(TOPDIR),)
  TOPDIR := $(CURDIR)
  PROJECT_NAME := $(notdir $(TOPDIR))
  PROJECT_HOME := $(dir $(TOPDIR))
  PROJECT_PATH := $(TOPDIR)
  PROJECT_LANGUAGES := C C++ Shell
endif

ENV0 :=  PROJECT_NAME PROJECT_HOME PROJECT_LANGUAGES PROJECT_PATH
export $(ENV0)

