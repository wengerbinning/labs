This element defines the document's title that is shown in a browser's title bar
or a page's tab. It only contains text; tags within the element are ignored.



## Attribute

This element only includes the global attributes.


## Notes

* The `<title>` element is always used within a page's `<head>` block.

## Links

* [Element Description](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/title)