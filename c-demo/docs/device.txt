
设备属性

* 基础(设备类型, 设备型号, 生产序列， 硬件版本)
* 通用(设备名称, 固件版本，设备地址)
* 区域(当前时区, 时区锁定)
* 语言(当前语言, 支持语言)
* 时间(当前时间, 启动时间)

设备状态

* 启动(已启动, 启动中)*
* 开发(已发布, 开发中)*
* 激活(未激活, 已激活)
* 注册(未注册, 已注册)
* 接管(未接管, 已接管)

* 关机(需关机, 关机中)*
* 重启(需重启, 重启中)*
* 重置(需重置, 重置中)*
* 升级(需升级, 升级中)*
